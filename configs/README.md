# CPose Configs

This folder stores the canonical configuration files used by the Flask app.

- `unified_config.yaml`: shared runtime values for the three project phases.
- `pose_adl.yaml`: thresholds and label definitions for Phase 3.
- `runtime.env.example`: optional environment template for local overrides.
