numpy<2                      # Nền tảng mảng số học; giữ <2 để tránh lỗi tương thích
opencv-python==4.9.0.80      # Xử lý ảnh/video bằng OpenCV
Pillow>=9.5.0                # Đọc/ghi/chuyển đổi ảnh kiểu PIL
PyYAML>=6.0                  # Đọc file cấu hình YAML
scipy>=1.10.0                # Hàm khoa học, tối ưu, xử lý tín hiệu
tqdm>=4.66.0                 # Thanh tiến trình khi train/infer

ultralytics>=8.2.0           # YOLO Ultralytics cho detect/track/infer
lapx>=0.5.2                  # Assignment solver cho tracking

yacs>=0.1.8                  # Quản lý config dạng node
gdown>=5.1.0                 # Tải file/model từ Google Drive
scikit-learn>=1.3.0          # ML utilities, preprocessing, metrics
faiss-cpu>=1.7.4             # Vector similarity search trên CPU
tensorboard>=2.14.0          # Xem log train
termcolor>=2.4.0             # In màu terminal
tabulate>=0.9.0              # In bảng đẹp trong terminal
Cython>=3.0.0                # Hỗ trợ build extension C/C++

openmim>=0.3.9               # Trình quản lý package cho OpenMMLab